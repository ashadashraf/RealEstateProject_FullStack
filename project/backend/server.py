# import uvicorn
# # from backend.asgi import application

# if __name__ == "__main__":
#     uvicorn.run("backend.asgi:application", host='0.0.0.0', port=5000, reload=True)
#     # uvicorn.run(application, host='0.0.0.0', port=5000, reload=True)
